ServerName localhost
ServerTokens Prod
ServerSignature Off

<Directory /var/www/html>
    Options -Indexes
    AllowOverride All
    Require all granted
</Directory>

# Nothing outside /api is served
RedirectMatch 404 ^/(?!api/).*$
